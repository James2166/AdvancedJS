var 
    // first name of the person (obviously)
    firstname, 
    
    // last name of the person (Again, its obvious)
    lastname, 
    
    // the language (it's right on the nose, just like the last two)
    // can be 'en' or 'es'
    language;

var person = {
    // John Doe is a multi use placeholder name given to people 
    // who's identity is unknown or being hidden
    firstname: 'John',

    // Jane Doe is the female Equivalent of John Doe
    Lastname: 'Doe'
}

console.log(person)